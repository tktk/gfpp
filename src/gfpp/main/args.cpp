#include "fgpp/util/export.h"
#include "gfpp/main/args.h"

#include "gfpp/boot/config.h"

namespace gf {
    fg::Args * newArgs(
        const BootConfig &
    )
    {
        return nullptr;
    }

    void free(
        fg::Args &
    )
    {
    }
}
